spring.application.name=stockknock

# Database Configuration (PostgreSQL)
spring.datasource.url=jdbc:postgresql://localhost:5432/stockknockdb
spring.datasource.username=sxxm
spring.datasource.password=sxxmpass
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.default_batch_fetch_size=100
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
# 테이블 이름을 엔티티의 @Table 어노테이션에 명시된 이름 그대로 사용 (복수형 변환 방지)
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# Server Configuration
server.port=8080

# CORS Configuration
spring.web.cors.allowed-origins=http://localhost:3000
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*

# ============================================
# 이메일 설정 (Gmail)
# ============================================
# ⚠️ 보안 권장사항:
# 1. App Password 사용: Gmail 설정 > 보안 > 2단계 인증 > 앱 비밀번호 생성
# 2. OAuth2 사용 (더 안전): application-prod.properties 참고
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Gmail OAuth2 설정 (선택사항, 더 안전)
# spring.mail.properties.mail.smtp.oauth2.enable=${GMAIL_OAUTH2_ENABLE:false}
# spring.mail.properties.mail.smtp.oauth2.client-id=${GMAIL_OAUTH2_CLIENT_ID:}
# spring.mail.properties.mail.smtp.oauth2.client-secret=${GMAIL_OAUTH2_CLIENT_SECRET:}
# spring.mail.properties.mail.smtp.oauth2.refresh-token=${GMAIL_OAUTH2_REFRESH_TOKEN:}
# spring.mail.properties.mail.smtp.oauth2.access-token-url=https://oauth2.googleapis.com/token

# ============================================
# GPT API 설정
# ============================================
gpt.api.key=${OPENAI_API_KEY}
gpt.api.url=${GPT_API_URL:https://api.openai.com/v1/chat/completions}
gpt.model=${GPT_MODEL:gpt-4o-mini}

# ============================================
# JWT Configuration
# ============================================
# ⚠️ 보안 경고: SKJWT_SECRET은 반드시 64 bytes 이상이어야 합니다!
# 생성 방법: openssl rand -base64 64 | tr -d '\n'
# 프로덕션 환경에서는 반드시 환경 변수로 설정하세요.
jwt.secret=${SKJWT_SECRET:stockknock-secret-key-change-in-production}
jwt.expiration=86400000

# Stock API Configuration (예: Alpha Vantage, Yahoo Finance 등)
stock.api.alpha-vantage.key=${ALPHA_VANTAGE_API_KEY:}
stock.api.yahoo-finance.enabled=true
stock.api.twelve-data.key=${TWELVE_DATA_API_KEY:}

# ============================================
# News API 설정
# ============================================
# ⚠️ NewsAPI 키 설정 필요:
# 1. https://newsapi.org/register 에서 무료 API 키 발급
# 2. 환경 변수 설정: export NEWS_API_KEY=your_api_key_here
# 3. 또는 IDE 실행 설정에서 환경 변수 추가
# 
# 무료 플랜 제한: 일일 100회 요청
# 개발자 플랜: 일일 250회 요청
news.api.newsapi.key=${NEWS_API_KEY:}
news.api.enabled=true

# 알림 설정
notification.email.enabled=${NOTIFICATION_EMAIL_ENABLED:false}

# ============================================
# FastAPI 설정
# ============================================
fastapi.base-url=${FASTAPI_BASE_URL:http://localhost:8000}

# ============================================
# Swagger/OpenAPI 설정
# ============================================
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
springdoc.swagger-ui.enabled=true
springdoc.api-docs.enabled=true
